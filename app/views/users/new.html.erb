<% provide(:title, 'Sign up') %>
<h1>Sign up</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@user) do |f| %>
	   <%= render 'shared/error_messages', object: f.object %>
	  <!-- Notice here that we render a partial called ’shared/error_messages’; this reflects the common Rails convention of using a 
	  dedicated shared/ directory for partials expected to be used in views across multiple controllers. 
	  (We’ll see this expectation fulfilled in Section 9.1.1.) 
	  This means that we have to create both the new app/views/shared directory and the _error_messages.html.erb partial file --> 
	  
      <%= f.label :name %>
      <%= f.text_field :name %>
      
      <!-- dwie linie powyzej wyprodukuja taki kod HTML:
      <label for="user_name">Name</label>
      <input id="user_name" name="user[name]" size="30" type="text" /> -->

      <%= f.label :email %>
      <%= f.text_field :email %>

      <%= f.label :password %>
      <%= f.password_field :password %>
      
      <!-- dwie linie powyzej wyprodukuja taki kod:
      <label for="user_password">Password</label><br />
      <input id="user_password" name="user[password]" size="30" type="password" /> -->

      <%= f.label :password_confirmation, "Confirmation" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "Create my account", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>